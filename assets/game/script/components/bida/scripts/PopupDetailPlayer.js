var Linker = require('Linker');

cc.Class({
    extends: cc.Component,

    properties: {
    },

    // LIFE-CYCLE CALLBACKS:

    onLoad () {
    },

    start () {

    },

    clickClosePopup () {
        this.node.active = false;
    },

    // update (dt) {},
});
